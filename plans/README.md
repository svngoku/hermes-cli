# Implementation Plans

Generated by the improve skill on 2026-07-10 (HEAD `970d1db`). Focus: production-ready CLI consistency and multi-GPU safety (minimal multi-GPU scope).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | GPU inventory + TP validation | P1 | S | — | DONE |
| 002 | Port preflight before serve | P1 | S | — | DONE |
| 003 | CUDA devices flag (minimal multi-GPU) | P1 | S | 001 | DONE |
| 004 | Daemon boot crash visibility | P1 | M | 002 | DONE |
| 005 | Command characterization tests | P2 | M | 001, 002, 003, 004 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **001 first**: introduces pure GPU/TP helpers and serve validation used by later plans.
- **002 independent of 001** but both touch `serve`/`run`; land 001 first to reduce merge conflict surface, or land carefully if parallel.
- **003 depends on 001**: validation must count devices after `CUDA_VISIBLE_DEVICES` filtering.
- **004 depends on 002**: daemon boot path should also refuse a busy port before detaching.
- **005 last**: characterization tests lock in the new preflight behavior; write after 001–004 so tests match final APIs.

## Recommended executor order

```
001 → 002 → 003 → 004 → 005
```

Run `just check` after each plan.

## Findings considered and not planned this round

- Bind-host `0.0.0.0` warning (finding 8): small follow-up; not selected.
- Install/venv path coupling (finding 9): medium risk, deferred.
- `logs` / `restart` commands (finding 10): product follow-up after daemon registry is solid.
- Full multi-GPU flags (PP, dtype, mem-fraction, auto-TP): out of minimal multi-GPU scope.
- Dead interactive form wiring: deferred; do not delete in these plans unless a plan explicitly says so.

## Verification baseline (all plans)

```bash
just check   # lint + go test -race + build
# or:
go vet ./... && go test -race ./... && CGO_ENABLED=0 go build -o bin/hermes ./cmd/hermes
```
